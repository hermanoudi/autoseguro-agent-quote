# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
*.egg-info/

# env / secrets (NUNCA commitar)
.env
.env.*
!.env.example
*.key
*.pem

# Estado de runtime do agent-service (trilha de auditoria por conversation_id,
# gerada a cada execucao local/demo — nao e codigo nem entrega, e um banco de dados)
agent-service/data/
agent-service/logs/

# OS / editores
.DS_Store
.idea/
.vscode/
*.log

# Claude Code (permissoes locais, especificas da maquina/sessao)
.claude/settings.local.json
